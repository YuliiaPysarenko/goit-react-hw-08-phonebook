import styled from 'styled-components';

const StyledBlock = styled.div`
  margin: 20px;
`;
const H1 = styled.h1`
  text-transform: uppercase;
`;
const H2 = styled.h2`
  font-weight: 500;
`;

export { StyledBlock, H1, H2 };